{
  "name": "my-agent",
  "version": "1.0.0",
  "description": "",
  "main": "dist/agent.js",
  "scripts": {
    "build": "tsc",
    "gcp-build": "npm run build",
    "start": "sh -c 'NODE_NO_WARNINGS=1 npx adk api_server dist/agent.js --host 0.0.0.0 --port ${PORT:-8080} --log_level error --compile=false --bundle=false'",
    "agent": "NODE_NO_WARNINGS=1 npx adk run agent.ts --log_level error",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@google/adk": "^0.6.1",
    "dotenv": "^17.2.3",
    "googleapis": "^157.0.0",
    "typescript": "^6.0.2",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@google/adk-devtools": "^0.6.1"
  }
}
